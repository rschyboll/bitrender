import { Card } from 'primereact/card';
import { memo } from 'react';

import './style.scss';

const RegisterPage = memo(function RegisterPage() {
  return (
    <Card id="register-card" className="shadow-2">
      Tests2
    </Card>
  );
});

export default RegisterPage;
